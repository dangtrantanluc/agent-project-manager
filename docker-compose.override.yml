services:
  backend:
    volumes:
      - ./backend:/app
      - ./agent:/app/agent
      - uploads_data:/app/uploads
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
